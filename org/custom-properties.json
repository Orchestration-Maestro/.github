[
  {
    "allowed_values": [
      "other",
      "rust",
      "workflows"
    ],
    "default_value": "other",
    "description": "Which central checks guard the default branch: 'rust' runs maestro-rust-workflows' ci.yml (ruleset rust-central), 'other' its hygiene.yml (hygiene-central), 'workflows' is maestro-rust-workflows, held to its own CI by its own ruleset.",
    "property_name": "stack",
    "require_explicit_values": false,
    "required": true,
    "value_type": "single_select",
    "values_editable_by": "org_actors"
  }
]
