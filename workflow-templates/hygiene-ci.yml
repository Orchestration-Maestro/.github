# generated by rust-gate sync; do not edit
name: CI
"on":
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  # Keep the job id `hygiene`: the hygiene-required ruleset requires the
  # check "hygiene / Required hygiene" before any merge to the default branch.
  hygiene:
    uses: Orchestration-Maestro/rust-workflows/.github/workflows/hygiene.yml@13f5a701c85420f4a7140071855d53f20c28bed6  # v3.0.1
    permissions:
      contents: read
