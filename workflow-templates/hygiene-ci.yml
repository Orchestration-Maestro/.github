# generated by rust-gate sync; do not edit
name: CI
"on":
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  # Keep the job id `hygiene`: the hygiene-required ruleset requires the
  # check "hygiene / Required hygiene" before any merge to the default branch.
  hygiene:
    uses: Orchestration-Maestro/rust-workflows/.github/workflows/hygiene.yml@be7aa1f3d0be144da78fba9331bdeb0038836025  # v2.3.0
    permissions:
      contents: read
