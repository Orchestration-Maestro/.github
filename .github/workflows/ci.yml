# generated by rust-gate sync; do not edit
name: CI
"on":
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  # Keep the job id `hygiene`: the hygiene-required ruleset requires the
  # check "hygiene / Required hygiene" before any merge to the default branch.
  hygiene:
    uses: Orchestration-Maestro/rust-workflows/.github/workflows/hygiene.yml@54f3a841e96398283c2b4156cbdcf4ebf7ef6563  # v2.0.0
    permissions:
      contents: read
